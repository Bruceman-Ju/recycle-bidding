spring:
  cloud:
    nacos:
      config:
        server-addr: ${NACOS_SERVER:localhost:8848}
        namespace: ${NACOS_NAMESPACE:}
        group: DEFAULT_GROUP
        extension-configs:
          - data-id: gateway-whitelist.yml
            group: DEFAULT_GROUP
            refresh: true
